import 'package:flutter/material.dart';

class BuatJanjiScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Buat Janji'),
        backgroundColor: Colors.green,
      ),
      body: Center(
        child: Text('Halaman Buat Janji'),
      ),
    );
  }
}
